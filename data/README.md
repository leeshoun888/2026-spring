# VOC analysis data

`voc-state.json` is created and updated by the app when analyses run.
It is intentionally stored in the repository so PoC analysis runs survive refreshes and local restarts.

API keys and private settings are stored separately under `.data/` and should not be committed.
